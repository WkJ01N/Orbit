# Orbit v1.3.2

**平台：** Windows · Android

## 重大修复

- Android 课前、打卡和次日摘要改由原生 `AlarmManager` 接收器直接发布通知，不再依赖后台启动 Flutter/Dart
- 修复 iQOO / OriginOS 回到桌面、锁屏、进程冻结、普通系统回收及最近任务上划后不触发提醒的问题
- 开机、应用更新和系统时间变化后从原生持久化记录恢复未到期提醒
- 升级时清理 1.3.1 遗留的 FLN 与 Dart AlarmManager 排程，避免重复通知
- 通知诊断工具移至“数据管理 → 调试”，一分钟测试使用与课程提醒相同的原生投递链
- 课表与“接下来的课程”页面将添加课程入口移至标题栏左侧
- 已在 iQOO V2408A 验证锁屏及最近任务上划后仍可提醒；系统设置中的真正强行停止仍无法绕过

## 其他

- 版本号 1.3.2（`pubspec.yaml` build `+13`）

---

# Orbit v1.3.0

**平台：** Windows · Android

## 新功能与改进

### 课程与数据安全

- 同一课程支持“仅本节 / 本节及以后 / 全部同课程”批量编辑和删除
- 新增保留 7 天的回收站、批量恢复、永久清空以及删除后的即时撤销
- 数据库升级至 v3，替换、批量删除和清空课表均改为可恢复的软删除
- 修复冷启动时“接下来的课程”遗漏正在上课课程的问题

### 备份与发布

- JSON 备份升级至 v2，包含课程、主题、提醒、课表显示和课程颜色设置，并兼容 v1
- 恢复时可选择课程或设置，以及合并覆盖或替换当前课表
- Android Release 改用正式签名配置；新增 GitHub Actions 检查、构建和标签发布流程
- 页面动画适配系统“减少动态效果”，数据管理从设置主页拆分为独立组件

### 界面与交互

- 设置页按语言、课表、提醒和数据分类，修复外观、主题色、系统及数据管理标题对齐
- 课表支持跟手左右滑动、回弹以及箭头和方向键统一过渡，并在翻页后保留时间轴位置
- 课程卡片统一使用 8px 圆角，更新手机、多日和整周视觉基线
- Android 1.3.0 启用全新正式发布证书；从 1.2.1 或更早版本升级时需先卸载旧版

## 其他

- 版本号 1.3.0（`pubspec.yaml` build `+10`）
- 小步升级 path_provider、sqflite_common_ffi、tray_manager 和 window_manager
- 测试增至 128 项，覆盖数据库迁移、批量课程操作、回收站、备份和分页动画

---

# Orbit v1.2.1

**平台：** Windows · Android

## 新功能与改进

### 课表

- 重做为按课程真实起止时间定位的自适应时间轴，按窗口宽度切换单日、多日与整周模式
- 同时段课程自动分栏，长课程按实际时长显示；课程名称、时间、地点和教师按空间优先级排布
- 修复首尾时间刻度裁切；当前时间提示线仅显示在当天列中
- 支持隐藏无课日期、连续跨周浏览以及动态多日显示数量

### 外观与课程颜色

- 新增「彩色」主题，为不同课程稳定分配颜色，支持浅色和深色模式
- 自定义课程颜色作用于同类课程，并可恢复当前主题默认颜色
- Windows 中文界面统一使用微软雅黑 UI，改善中英文混排字体粗细不一致

### 接下来与本地化

- 每周首节课程前显示该周日期；倒计时旁可选显示日期、星期或两者
- 修复语言切换完成提示仍使用切换前语言的问题
- 简化课程删除按钮文案，补齐简体中文、繁体中文和英文文案

## 其他

- 版本号 1.2.1（`pubspec.yaml` build `+9`）
- 测试增至 109 项，包含三档课表视觉回归、时间轴边界、课程配色和接下来日期显示

---

# Orbit v1.2.0

**平台：** Windows · Android

## 新功能与改进

### 设置与外观

- 深浅色模式可选手动切换（跟随系统 / 浅色 / 深色）
- 课表周起始日可选周一或周日
- 课表显示密度可选（紧凑 / 标准 / 宽松）
- 课程可按科目自定义颜色（课程详情页设置，课表与「接下来」同步显示）

### 提醒

- 课前提醒、打卡提醒支持自定义标题 / 正文模板（占位符 `{course}`、`{room}`、`{time}`、`{minutes}` 等）；留空使用默认文案

## 优化

- 提醒重排优先复用已缓存课表数据，减少冷启动重复查库
- 日程摘要按日分组一次扫描，替代逐日全表过滤
- 导入合并冲突检测按日期分组，降低 O(n×m) 比较开销
- `replaceSessions` 批量 DELETE 替代逐条删除
- 提取重复的 Android 通知详情构造逻辑

## 其他

- 版本号 1.2.0（`pubspec.yaml` build `+8`）
- 测试增至 97 项（含模板渲染、周起始日、课程颜色、显示密度）

---

# Orbit v1.1.2

**平台：** Windows · Android

## 新功能与改进

### 提醒

- 次日课表确认支持自定义标题 / 正文模板（占位符 `{count}`、`{time}`、`{date}`）；留空使用默认文案
- 新增「无课时也提醒」开关；关闭后明日无课时不注册通知
- 全局提醒重同步横幅：区分 pending 校验失败、部分 AlarmManager 失败与真正异常

### Android

- 次日摘要纳入 `AlarmManager.oneShotAt` 主通道，与课前 / 打卡一致；FLN 同步使用 `alarmClock` + 最高优先级
- 课前 / 打卡以 AlarmManager 为主通道，避免 FLN + Timer + AlarmManager 三重投递
- `scheduleReminderAlarms` 先 OS 注册成功再写 registry；维护 / 测试闹钟 ID 迁至独立区间
- 清空全部数据时同步取消全部 AlarmManager 闹钟

### 课表

- 宽屏 pinned 表头 delegate 按周次与列宽比较，减少无效重建；吸顶时增加底部分割线
- 分钟级时间 tick 仅重建红线 / Chip 高亮子树，整表不再每分钟 rebuild
- 换周清理 Chip GlobalKey 与跨周选中态

## 修复

- 修复 Android 次日课表提醒仅走低优先级 FLN、后台 / 杀进程后夜间不触发的问题
- `_rescheduleChain` 单次失败后不再永久阻断后续排程
- AlarmManager registry JSON 损坏时降级清空，避免后台 isolate 崩溃
- Sheet 关闭后 SnackBar 使用 root messenger，保存结果提示不再丢失
- 通知点击 handler 在 dispose 时注销
- 导出 / 备份与设置页 async 间隙补充 `context.mounted` 检查
- Provider 异步加载使用 generation token，减轻首帧闪烁与乱序写 state

## 其他

- 版本号 1.1.2（`pubspec.yaml` build `+7`）
- 测试增至 86 项（含次日摘要 builder、resync 状态解析、版本号同步）

---

# Orbit v1.1.1

**平台：** Windows · Android

## 新功能与改进

### Android

- 课前 / 打卡提醒通过 `AlarmManager.oneShotAt` 逐条注册，App 退到后台或被系统杀进程后仍可触发
- 每 6 小时维护闹钟，设备重启后自动恢复排程
- 设置页新增「后台提醒（Android）」区块：电池优化开关、OriginOS / iQOO 自启动提示、「测试后台提醒（1 分钟）」按钮

### 课表

- 宽屏表头改用 `SliverPersistentHeader` 吸顶，与表体共享同一垂直滚动 viewport；导入多时段课表出现纵向滚动条时，周日期与下方课程列仍对齐

## 修复

- 修复宽屏导入课表后纵向滚动条占用表体宽度、导致表头与课程列水平错位的问题

## 其他

- 版本号 1.1.1（`pubspec.yaml` build `+6`）
- 测试增至 76 项（含宽屏表头 / 表体对齐、AlarmManager 注册表用例）

---

# Orbit v1.1.0

**平台：** Windows · Android

## 新功能与改进

### 课表

- 宽屏模式下表头与课程列对齐（统一横向滚动，消除滚动条占位错位）
- 左侧时间线各时段固定行高，不再因单日课程变化而拉伸
- 当前时间红线仅在课表有效时段内显示，并按真实时间比例定位；**且仅在今天对应列显示**（窄屏需选中今天，宽屏不横跨整周）
- 已导入课表后，无课周不再显示「立即导入」按钮；副标题改为引导切换周次
- 课表 AppBar 窄屏布局优化：换周按钮与搜索 / 今天按钮不再重叠
- 课表 AppBar 统一为标准 AppBar，与其他页面顶栏样式一致
- 横向滚动检测边缘状态时不再触发整表 rebuild，宽屏横滚更顺滑
- 课程 Chip 高亮与当前时间红线共用分钟级时间源，每分钟同步更新
- 时间红线、删除菜单改用主题色；课程 Chip 字号继承 `textTheme`，支持系统字号缩放
- 课表加载态改为骨架屏占位，减少白屏跳变
- 课表单元格增加重绘隔离（`RepaintBoundary`），降低时间线更新时的绘制开销

### 接下来

- 「即将开始」与倒计时分两行显示，避免窄列内换行错乱
- 「上课中」状态徽章垂直居中对齐
- 列表扁平化懒加载，去除 `IntrinsicHeight`，长列表滚动更流畅
- 加载态改为骨架屏占位

### 课程

- 手动添加 / 编辑课程时可填写学院名称；详情页学院为空时不显示该行
- 手动课程未填编号时使用唯一内部编号，避免同日同时间冲突覆盖

### 界面与导航

- 底部 Tab 切换增加淡入过渡（保留 IndexedStack 页面状态）
- 宽屏（≥600px）课程详情、编辑、备注改为居中对话框；窄屏保持底部 Sheet
- 课表与「接下来」FAB 仅在数据加载完成后显示，行为一致

### 提醒

- 保存课程后显示「已排定 N 条提醒」；系统未写入 pending 时明确提示检查精确闹钟、电池优化与自启动（OriginOS / iQOO）
- 精确闹钟调度失败时自动降级为非精确模式，降低完全无提醒的概率
- 课前 / 打卡提醒优先使用 Android `alarmClock` 模式，并保留精确 / 非精确降级链
- 2 小时内到期的课前 / 打卡提醒额外注册进程内 Timer，App 存活时直接弹出通知作为兜底
- 使用设备 IANA 时区（`flutter_timezone`）配置提醒调度，减少时区映射偏差
- 「重新同步提醒」在仅开启签到 / 次日摘要时也可使用
- 手动添加课程默认开始时间为下一整点或半点，减少因时间已过去而静默跳过提醒

### Android

- 系统闹钟增加 `SET_ALARM` 包可见性声明，并支持 OriginOS / Vivo 时钟 Intent 回退

## 稳定性

- 修复冷启动时提醒排程未使用已保存设置的问题
- 排程完成后校验系统 pending 数量，捕获 OEM 静默丢弃精确闹钟的情况

## 修复

- 修复当前时间红线在非今天列 / 非选中天仍显示的问题
- 修复课程详情弹窗删除时报错「Cannot use ref after the widget was disposed」
- 修复手动添加课程无法填写学院信息
- 修复手动课程提醒已排入 pending 但到点不触发（时区、Android 投递链与近端 Timer 兜底）
- 修复课前 / 打卡提醒对手动添加与导入课程不生效（含排程验证与权限相关改进）
- 修复课程详情页编辑、备注按钮点击无效（删除见上）
- 修复 Android 系统闹钟「无法打开系统闹钟」
- 修复 PC 宽屏手动添加课程后表头与列不对齐

## 其他

- 版本号 1.1.0（`pubspec.yaml` build `+5`）
- 测试增至 70 项（含红线当天列、提醒时区 / 近端兜底、倒计时与 Chip 状态用例）

---

# Orbit v1.0.3

**平台：** Windows · Android

## 新功能与改进

### 导入与课表

- 导入时若与本地存在重复周，可选择「整周替换」或「合并并覆盖冲突」
- 手动添加 / 编辑课程时，时间冲突自动覆盖旧课，并提示覆盖数量
- 设置中新增默认周次：智能 / 本周 / 最早有课周（默认智能）
- 课程搜索支持简体与繁体中文互搜；搜索页增加加载态、错误提示与结果截断说明
- 当周无课时提供「立即导入」入口

### 提醒

- 课表增删改后若提醒重排部分失败，SnackBar 提示失败数量
- 提醒重排部分失败时在全局（设置页除外）显示 MaterialBanner，便于重新同步
- Android 权限引导在未允许精确闹钟时跳转精确闹钟授权页

### Windows

- 修复长时间锁屏或合盖后托盘图标偶发消失（升级 tray_manager，唤醒 / 聚焦时自检恢复）
- 点击系统通知时自动唤出主窗口
- 首次关闭到托盘前先显示提示 SnackBar
- 托盘菜单与 tooltip 随语言切换更新

### 设置与界面

- 导出 / 备份 / 还原操作增加进行中状态，防止重复点击
- 课程编辑与备注保存按钮增加防双击
- 清除课表、批量删除、开机启动等失败时使用语义化错误文案

## 稳定性

- 提醒重排部分失败时写入全局状态，避免静默丢失提醒而无感知

## 修复

- 修复从搜索结果打开课程详情可能失败
- 修复 Android 导出取消仍显示成功
- 修复 Android 精确闹钟权限跳转不准确

## 其他

- 版本号升至 1.0.3（`pubspec.yaml` build `+4`）
- 测试增至 54 项（含简繁搜索用例）

---

# Orbit v1.0.2

**平台：** Windows · Android

## 新功能与改进

### 导入与导出

- 支持将课表导出为 JSON 或 xlsx，便于备份与迁移
- 支持从 JSON 备份恢复课表
- 导入页增加 xlsx 格式说明；解析失败时显示本地化错误提示

### 课表

- 支持手动添加与编辑单节课程（课表网格与「接下来」页 FAB）
- 新增课程搜索，可按科目、课室、教师等关键词查找

### 提醒

- 点击课前通知可跳转至对应课程详情
- Android 电池优化豁免改为可双向开关，并与系统真实状态同步
- Android 提醒相关权限分项展示授权状态，可跳转系统设置
- 启用 `RebootBroadcastReceiver`，设备重启后自动维护闹钟

### 设置与界面

- 提醒开关操作失败时显示 SnackBar 反馈
- 提醒重排失败时在设置页显示 MaterialBanner 提示

## 稳定性

- 提醒重排（`rescheduleAll`）串行化，避免并发竞态
- 后台维护回调增加 try/catch 与调试日志
- 冷启动时通知 payload 延后至设置初始化完成后再处理
- 应用回到前台时以 6 小时 debounce 重新同步提醒
- 时区配置统一至 `timezone_utils`（UTC 偏移，适配澳门 / 中国大陆）

## 修复

- 修复 Android 电池优化豁免开启后无法关闭的问题
- 语言切换后自动 resync 提醒与界面
- 改进删除、导入等操作的错误处理

## 其他

- 版本号升至 1.0.2（`pubspec.yaml` build `+3`）
- 测试增至 51 项（含备份往返与时区配置用例）

---

# Orbit v1.0.1

**平台：** Windows · Android

## 新功能与改进

### 课表

- 支持左右滑动切换周次
- 修复周切换时页面闪烁
- 有课表但当前周无课程时，仍可翻页浏览其他周

### 界面

- 课程详情底部按钮窄屏自适应（编辑 / 备注 / 删除）
- 课表星期选择器垂直对齐优化
- 「接下来」列表状态竖条对齐优化

### Windows

- 单实例启动：重复打开时激活已有窗口，不新增任务栏图标
- 托盘「退出」加速至约 1 秒内
- 应用显示名称统一为 **Orbit**

## 修复

- 修复设置中「清除所有课表」后，课表页未回到「尚未导入课表」空态的问题
- 修复课表翻页时的短暂闪烁

#!/bin/sh
# Strip Cursor agent attribution so it does not appear on GitHub Contributors.
sed -i.bak '/cursoragent@cursor\.com/d' "$1" 2>/dev/null || sed -i '' '/cursoragent@cursor\.com/d' "$1"
rm -f "$1.bak"

{
  "read": "auth != null && (resource.openid == auth.openid || resource.openid == auth.uid)",
  "write": "auth != null && auth.loginType != 'ANONYMOUS' && (resource.openid == auth.openid || resource.openid == auth.uid)"
}
